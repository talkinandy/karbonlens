# Changelog

All notable changes to KarbonLens are recorded here. Entries are grouped by release and ordered newest-first.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). SemVer per `VERSION` once we ship v0.1.

---

## [Unreleased] — v0.1 integration branch (`feature/v0.1-impl`)

Entries land here as individual user stories (T01–T23) complete against acceptance criteria. See `docs/stories/` for the story specs and `docs/TASKS.md` for the sprint overview.

### Added
- **T01 VPS foundation** ([feature/v0.1-impl]) — provisioned the Hetzner CX32 box: PostgreSQL 16.13 + PostGIS 3.6.3, `pg_trgm` 1.6 + `pgcrypto` 1.3 extensions in the `karbonlens` database, `karbonlens` Unix user (no-login, uid 999) and Postgres role (scram-sha-256 on loopback only — wrong-password rejection verified with `FATAL: password authentication failed` exit 2). Standard directories `/opt/karbonlens` and `/var/{log,lib}/karbonlens` owned by the karbonlens user. Postgres bound to `localhost` only (`listen_addresses = 'localhost'`; no public IP exposure). Secrets at `/root/karbonlens-secrets.txt` (chmod 600, root:root). Runbook at `docs/runbooks/vps-setup.md`; implementation report at `docs/stories/reports/T01-implementation-report.md`. Code-audit verdict: PASS-WITH-FIXES (SHA 5c5bbd8).
- **T02 Schema migration 001** ([feature/T02-schema-migration]) — 15 tables (projects, registries, issuances, retirements, idx_monthly_snapshots, satellite_alerts, regulatory_events, project_scores, project_match_queue, users, accounts, sessions, verification_tokens, notifications, schema_migrations) with PostGIS geography columns, `users.email_verified` for NextAuth v5 adapter, composite PKs on `verification_tokens` and `project_scores`, generated `projects.total_vcus_available`, cascade policies per architecture §3. All 11 indexes present (2 GIST). Tables owned by `karbonlens` role so future `ALTER TABLE` migrations succeed. Migration file: `scrapers/migrations/001_init.sql`; idempotent under `--single-transaction`.
- **T04 Drizzle schema + DB client** ([feature/T04-drizzle-schema]) — TypeScript Drizzle schema at `lib/schema.ts` mirrors all 15 tables from migration 001 (including `users.email_verified`, generated `projects.total_vcus_available`, composite PKs on `project_scores` and `verification_tokens`, `check` constraint on integrity score, PostGIS `geographyPoint` custom type for centroid + satellite-alert location). Singleton client at `lib/db.ts` enforces `DATABASE_URL` at module load. `/api/health` returns sanitized `{ok,db,error}` with HTTP 503 on failure and a classifier that detects auth-failed vs connection-refused without leaking connection strings. `drizzle.config.ts` scoped to introspect-only (SQL files under `scrapers/migrations/` remain authoritative). Post-audit fix round 1: 6 `accounts` token fields renamed to snake_case JS keys to match `@auth/drizzle-adapter` v1.11.2 contract; classifier extended to walk `err.cause.code` for ECONNREFUSED detection.
- **T03 Next.js 15 bootstrap** ([feature/T03-nextjs-bootstrap]) — scaffold Next.js 16 (App Router) at the repo root, Tailwind v4 CSS-first with tokens extracted from the legacy prototype, six screens ported with mock data under route groups `(public)` and `(app)`, `netlify.toml` wired for `@netlify/plugin-nextjs`, sole ownership of `.env.example` with eight keys mapped to consuming stories. Static prototype preserved at `legacy/prototype/`. Visual design parity not verified in a browser — follow-up needed at T11–T18. Route layouts keep `<html>` / `<body>` in root only.
- **T06 Verra scraper** ([feature/T06-verra-scraper]) — Python 3.12 scraper ingests Indonesian VCS projects into `projects` + `registries` + `issuances` via Verra's internal OData API (reverse-engineered from the Angular SPA since the documented URLs returned a shell). 64 Indonesian projects populated (>=40 target from PRD); 307 issuances across all vintages; idempotent on re-run. Entity resolution via `pg_trgm` fuzzy matching with a human-in-the-loop `project_match_queue` for 0.70–0.95 similarity band. Sole owner of `scrapers/pyproject.toml` with all Phase 2 runtime deps (psycopg, httpx, bs4, lxml, shapely, pyproj, pdfplumber, structlog, python-dotenv). Shared helpers at `scrapers/common/` for downstream scrapers to consume. Known limitations: Verra API exposes registration_date not PDD validation_date (T09 scoring will reflect this); status-map covers the 4 common Verra statuses but ~18 projects currently labeled `suspended` when they're in verification — follow-up T06.1 will extend the map before T11/T12 frontend. Two match-queue entries pre-resolved as `rejected` (legitimately distinct projects).
- **T10 Regulatory events seed v1** ([feature/v0.1-impl]) — 10 hand-curated Indonesian carbon regulations seeded into `regulatory_events` with bilingual summaries (EN + ID), importance classifications (`critical`/`high`/`medium`/`low`), and ad-hoc tags (T15 will build filter UI via `SELECT DISTINCT unnest(tags)`). Idempotent re-apply via `WHERE NOT EXISTS` dedupe; sentinel rows (N/A, TBD) use 3-field keys. `DO $$ ASSERT $$` block in the SQL rolls back the transaction if importance is typo'd. Post-audit fixes landed: row 1 name corrected (Dewan Nasional, not Badan Pengendalian), row 6 tag `mrvb` → `mrv`. **Status: `done-pending-factcheck` — rows 6–10 (Permenhut 7/2024, Kepmen LH 20/2025, Perpres 110/2025, Permenhut 6/2026, IDX 2026-07-01 forecast) await Andy's confirmation of document numbers and dates against authoritative sources. Code-correct; fact-correct pending.**
- **T05 NextAuth v5 + Google OAuth** ([feature/T05-nextauth-google]) — NextAuth v5 wired to the Drizzle adapter with `strategy: 'database'`, explicit adapter table bindings (`usersTable`, `accountsTable`, `sessionsTable`, `verificationTokensTable`) to match our plural schema names. Protected routes `/projects`, `/prices`, `/regulatory`, `/alerts` redirect unauthed to `/?signin=1`; three public project slugs exempt (`katingan-peatland`, `sumatra-merang-peat`, `rimba-raya`). Onboarding modal collects `persona` + `organization` on first authed render with a 7-day dismiss-snooze cookie. Sign-in and sign-out components, user avatar menu, session type augmentation, and a GCP-setup runbook all in place. **Phase A complete (code, build, middleware, unauthed redirects, API boundary); Phase B (live Google OAuth round-trip including T04's snake_case adapter-token-field verification) pending Andy's GCP credentials — see `docs/stories/reports/T05-implementation-report.md` §Phase-B.**
- **T07 GFW alerts scraper + migration 002** ([feature/T07-gfw-alerts-scraper]) — Python scraper registers 10km geodesic buffers with GFW's `/geostore` endpoint, queries `gfw_integrated_alerts` for each project, writes to `satellite_alerts` + fans out to `notifications` for `email_digest_opt_in` users. Migration 002 adds `projects.gfw_geostore_id` plus three expression-based unique indexes enabling `ON CONFLICT` dedupe on satellite_alerts, issuances (future-proofing T06), and notifications. 55 of 64 projects' `gfw_geostore_id` populated during implementation (GFW geostore POST accepts anonymous requests; query endpoint still requires a key — caching accelerates Phase B). Rate limit: 1 req/sec before each API call (both geostore + query), 3× exponential backoff on 5xx, hard-fail on 401 with runbook pointer. **Phase A complete (code, migration, dry-run, placeholder-key guard, dedupe mechanics live-verified); Phase B (real alerts ingested with GFW API key) pending Andy's key — see `docs/stories/reports/T07-implementation-report.md` §Phase-B checklist.** Post-audit fix round 1: `ON CONFLICT ON CONSTRAINT` replaced with column-list form because expression-based unique indexes cannot serve as constraint targets; added stale-geostore fallback so query-time 404 triggers re-registration.
- **T09 Score computation daily job** ([feature/T09-score-computation]) — Python job computes integrity scores for all 64 Indonesian projects daily. Four pillar sub-scores (validation_recency, reversal_risk, community_flags, transparency) combined via weighted average (`0.25/0.35/0.20/0.20`), methodology_version `v1` labeled everywhere. Zero-data trap fix: `reversal_score=50` (unknown-neutral) when no GFW satellite coverage; integrity clamped ≤60 when project has zero registries. Hardcoded community overrides (currently 1 of 3 slugs matches T06's real output — Andy to confirm the other two). `lib/score.ts` is the canonical TypeScript mirror of the Python scoring functions; both share the `ScoreComponents` type (T09's narrow touch to `lib/schema.ts` removed the placeholder and re-imports from `./score`). Python↔TS parity hand-verified: 11 test cases across sub-scores + edge cases all match exactly. **Phase A complete: 64 score rows in `project_scores` today; min=56, max=86, median=74. AC-5 (Rimba Raya 50-65) is environment-conditional — with empty `satellite_alerts` (T07 Phase B pending), reversal_score=100 inflates Rimba Raya's composite to 72. Will re-verify after Phase B alerts populate.**
- **T08 IDXCarbon monthly PDF scraper** ([feature/T08-idxcarbon-pdf-scraper]) — Python scraper downloads IDXCarbon monthly PDFs from `idxcarbon.co.id/data-monthly`, parses via `pdfplumber` with regex extractors for both old (2023-2024) and new (2025+) formats, upserts to `idx_monthly_snapshots`. 10 months ingested (Jun 2025 – Mar 2026) — IDXCarbon's current listing only exposes 10 months, so the PRD's ≥24 target is environmentally capped; flagged as T08 follow-up. Idempotent `ON CONFLICT (period_month) DO UPDATE` so corrected PDFs re-ingest cleanly. Helper `extract_field(text, pattern) -> int | None` disambiguates "no regex match" (raises `ParseError`) from "matched zero" (valid); caller must use `is None`, not truthiness. `avg_price_idr` derived (total_value / total_volume). Archive at `/var/lib/karbonlens/pdf-archive/`. Post-audit fix round 1: reconciled T06's canonical `scrapers/common/` helpers (the stubs used during parallel implementation had API drift — rebase + `configure_logging()` call + context-manager usage).

- **T14 Price intelligence screen** — `/prices` wired to real `idx_monthly_snapshots` (10 months Jun 2025 – Mar 2026). Server-component hero stats (latest month period + volume + value + avg price + MoM delta arrow/color). Dual-axis `recharts` `<ComposedChart>`: avg price on Y1, volume on Y2. 10-row detail table with PDF download links. Loading skeleton. Jan 2026 avg price Rp 40,025; Mar 2026 MoM ↓ 42.4% (Feb 74,020 → Mar 42,660). Inline audit performed in lieu of formal audit gate (implementer landed directly on feature/v0.1-impl): all 8 ACs pass, `tsc` + `build` clean, no forbidden files touched. `recharts ^2.13` added to package.json (resolved 2.15.4). Note: formal audit-gate process discipline should be restored for T17/T18 — `docs/retros/phase-3.md` to record the lesson.
- **T15 Regulatory timeline screen** — `/regulatory` wired to real `regulatory_events` (8 rows post-T10 fact-check). Server-rendered chronological timeline with year rails (suppressed when 0 cards visible), importance color stripe + pill (critical/high/medium/low), bilingual EN/ID content with ARIA tablist language toggle, dynamic ministry dropdown (4 distinct: IDX/Kemenhut/OJK/Presidential) + dynamic tag pills (27 distinct), repeated-param URL serialisation for multi-select, "Clear all" CTA when ≥1 filter active, forecast badge + "Coming up" section for is_upcoming rows (IDX full-launch placeholder). External source links open in new tabs with `rel="noopener noreferrer"`. Loading skeleton + semantic `<article>` + `<time datetime>`.
- **T06.1 Status normalization — follow-up** ([migration 003]) — closes the T06 known limitation where `projects.status` and `registries.status` held 11 distinct raw Verra `resourceStatus` strings. New `STATUS_MAP` in `scrapers/verra/fetch.py` covers all 17 globally-observed Verra statuses (11 present in Indonesia); `_map_status()` now returns `None` for unknown values with a `status_unmapped` WARNING log instead of silently defaulting to `'suspended'`. `upsert_registry_row()` now writes the canonical `status_db` instead of the raw `status_raw` string (one-line fix). Migration 003 adds a defensive UPDATE + a `CHECK (status IS NULL OR status IN ('active','pipeline','suspended','flagged'))` constraint on `projects`, enforcing the enum and making future Verra-response drift fail loudly. **Result:** status distribution changed `4 active / 28 pipeline / 29 suspended / 3 flagged` → `5 active / 54 pipeline / 2 suspended / 3 flagged` — a far more accurate picture (most Indonesian Verra projects are in verification cycles, not "suspended"). Katingan, Rimba Raya, and Sumatra Merang now correctly `pipeline` (Verra has them as "Verification approval requested" and "Late to verify"). T09 transparency differentiated at last: 5 projects in the 70-bucket (active), 59 still at 55 (correctly reflecting their pending-verification state).

### Changed

### Fixed
- **T05 Google provider auto-discovery fix** ([fa51104]) — `providers: [Google]` with no args relied on Auth.js v5 auto-detection from `AUTH_GOOGLE_ID` / `AUTH_GOOGLE_SECRET` env vars, but the runbook and `.env.example` use `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`. Result: `client_id=undefined` in the OAuth authorization URL and `invalid_client` from Google on every sign-in attempt. Fixed by passing `clientId` / `clientSecret` explicitly to the `Google(...)` provider. T05 Phase B verified end-to-end on 2026-04-21: user + account + session rows populate correctly; `accounts.access_token` and `accounts.id_token` persist under snake_case JS keys (live validation of T04's adapter-field-name fix). Known residuals: `users.email_verified` not populated by the default adapter mapping; `accounts.refresh_token` empty because the Google provider isn't configured with `access_type=offline`. Neither blocks v0.1; tracked for v0.2.
- **T07 GFW query `Origin` header fix — T07 Phase B complete** — live run exposed that GFW validates the API key's domain allowlist against the request's `Origin` header, not just the key string. Without `Origin`, the query endpoint returned 403 "Request is missing valid API key" while the geostore POST succeeded (different auth policy). Added `Origin: http://localhost` to `register_geostore()` and `query_alerts()` headers (our key's allowlist is `localhost` + `karbonlens.netlify.app`). **Phase B live-verified 2026-04-21:** 246,576 satellite alerts ingested across 60 projects (4 of 64 had no alerts in the 90-day window), 60 in-app notifications fanned out to the signed-in user, 0 errors. T09 AC-5 now passes: Rimba Raya integrity_score = 62 ∈ [50, 65]; Katingan = 68 ∈ [60, 85]. Score distribution across all 64 projects: min 58, max 86, mean 65.4. T07 status flipped `done-phase-a` → `done`.
- **T10 fact-check corrections** — adversarial fact-check against primary Indonesian regulatory sources (peraturan.go.id, peraturan.bpk.go.id, jdih.menlhk.go.id, kehutanan.go.id, antaranews.com) found 2 of the 10 seeded events unverifiable and 3 with incorrect dates/titles. **Removed:** Permenhut 7/2024 (SRN-PPI operationalization — not indexed on peraturan.bpk.go.id; only Permenhut 1/2024 exists for 2024 Kemenhut; correct SRN doc may be Permen LHK 7/2023 from KLHK but needs independent verification before re-seeding). Kepmen LH 20/2025 (DRAM/DPP — no primary source found; Permenhut 20/2025 exists but covers forest-area planning, unrelated). **Corrected:** Perpres 110/2025 event_date `2025-04-22 → 2025-10-10`, title updated to actual "Penyelenggaraan Instrumen NEK dan Pengendalian Emisi GRK Nasional" (it overhauls the NEK framework per Perpres 98/2021, not merely "re-opens international trade"). Permenhut 6/2026 event_date `2026-01-14 → 2026-04-13` (promulgation date; signed 6 April 2026), title updated to "Tata Cara Perdagangan Karbon Melalui Offset Emisi GRK Sektor Kehutanan" (implementing regulation for Perpres 110/2025 in forestry — not a moratorium lift). IDX full launch event_date `2026-07-01 → 2026-12-31` placeholder with title marked "(tanggal belum diumumkan)" and importance downgraded `high → medium` because as of January 2026 IEEFA reports allowance allocation delayed with no official date. **Net:** `regulatory_events` row count 10 → 8; seed file `scrapers/seed/regulatory_events_v1.sql` idempotent on re-apply; T15 timeline will render accurate post-correction data. T10 status flipped `done-pending-factcheck` → `done`.
- **T05.1 Middleware → proxy.ts migration + real public slugs** — surfaced by the T12 code-audit. Two fixes in one. (1) Next.js 16 deprecated the `middleware.ts` file convention in favour of `proxy.ts` (same API, just renamed) — the dev server was emitting a deprecation warning on every start. `middleware.ts` renamed to `proxy.ts`; `auth()` wrapper + `config.matcher` unchanged. Deprecation warning gone. (2) `PUBLIC_PROJECT_SLUGS` carried stale placeholder values (`katingan-peatland`, `sumatra-merang-peat`, `rimba-raya`) from the T05 spec draft — these never matched any real DB row after T06's canonical slugger landed. Updated to the three real flagship slugs: `katingan-peatland-restoration-and-conservation-project`, `sumatra-merang-peatland-project-smpp`, `rimba-raya-biodiversity-reserve-project`. Live-verified: the three flagship slugs now pass through unauth'd (no 307); all other `/projects/[slug]` routes correctly 307 to `/?signin=1`; `/prices`, `/regulatory`, `/alerts` unchanged. (Note: T12's `app/(app)/projects/[slug]/page.tsx` hasn't merged yet — the flagships currently 404 because the T03-scaffold mock page doesn't know these slugs; will resolve on T12 merge.)

---
